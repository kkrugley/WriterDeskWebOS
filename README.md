# WriterDesk WebOS

![License](https://img.shields.io/badge/license-GNU_GPL_v3-blue.svg)
![React](https://img.shields.io/badge/react-18.2-61DAFB.svg)
![Vite](https://img.shields.io/badge/vite-4.0-646CFF.svg)
![Tailwind](https://img.shields.io/badge/tailwind-3.3-38B2AC.svg)

> **WriterDesk WebOS** — это веб-симуляция автономной среды для писателей.
> Минимализм. Монохром. Полная концентрация. Air-Gap передача данных.

Проект реализует интерфейс специализированного устройства для набора текста в браузере, имитируя работу локальной файловой системы и аппаратных клавиш, а также внедряет уникальный протокол передачи данных через видео-поток QR-кодов.

---

## ✨ Особенности

### 🧬 Философия "Distraction Free"
*   **Монохромный интерфейс:** Высококонтрастная цветовая схема (WCAG AA), имитирующая e-ink дисплеи.
*   **Отсутствие уведомлений:** Полная изоляция от внешнего мира.
*   **Фокус на тексте:** Никаких лишних панелей инструментов. Только вы и курсор.

### 🖥️ WebOS Эмуляция
*   **Mock File System:** Создание, удаление, переименование папок и файлов (данные хранятся в `localStorage`).
*   **Device Bar & Status Bar:** Отображение заряда батареи, времени, статистики слов и состояния файла в реальном времени.
*   **Клавиатурное управление:** Полная поддержка горячих клавиш (`Ctrl+S`, `F2`, `Esc` и др.), описанных в документации устройства.

### 📡 Технология "QR-Stream" (Air-Gap)
Уникальная реализация однонаправленной передачи данных для изолированных сред:
1.  **Экспорт (Desktop):** Документ разбивается на чанки, хешируется (SHA-256) и транслируется в виде анимации QR-кодов (2.5 FPS).
2.  **Импорт (Mobile):** Камера считывает поток, собирает пакеты, проверяет целостность хеша и восстанавливает документ.

---

## 🛠️ Технический стек

*   **Core:** React 18 + Vite
*   **Styling:** Tailwind CSS (Custom Config)
*   **Cryptography:** `crypto-js` (SHA-256 verification)
*   **QR Protocol:** `react-qr-code` (Render), `html5-qrcode` (Scan)
*   **Icons:** Lucide React

---

## 🚀 Установка и запуск

```bash
# 1. Клонирование репозитория
git clone https://github.com/your-username/writerdesk-webos.git

# 2. Переход в директорию
cd writerdesk-webos

# 3. Установка зависимостей
npm install

# 4. Запуск режима разработки
npm run dev
```

Приложение будет доступно по адресу `http://localhost:5173`.

---

## ⌨️ Управление (Shortcuts)

Приложение перехватывает системные нажатия для полной имитации устройства:

| Клавиша | Действие | Контекст |
| :--- | :--- | :--- |
| **Ctrl + S** | Сохранить файл | Редактор |
| **Ctrl + Q** | Выйти без сохранения | Редактор |
| **F2** | Переименовать | Файловый менеджер |
| **F3** | Новый файл | Файловый менеджер |
| **F6** | Экспорт (QR-Stream) | Редактор |
| **Esc** | Отмена / На уровень вверх | Глобально |

---

## 📂 Структура проекта

```
src/
├── components/
│   ├── FileSystem/    # Логика файлового менеджера
│   ├── Editor/        # Текстовый процессор
│   ├── QR/            # Модули экспорта и импорта (Stream)
│   └── Layout/        # Системные панели (Status/Device Bars)
├── hooks/             # Custom Hooks (useFileSystem, useQRStream)
├── utils/             # Логика протокола, хеширование, форматтеры
└── ...
```

---

## 🔒 Безопасность протокола

Передача данных через QR-Stream защищена на уровне целостности:
1.  **Сериализация:** Base64 кодирование для поддержки Unicode.
2.  **Чанкинг:** Разбивка на пакеты по 250 байт.
3.  **Верификация:** В каждый пакет зашит SHA-256 хеш оригинального файла. Сборка файла невозможна, если хеш не совпадет.

---

## 📄 Лицензия

Распространяется под лицензией GNU GENERAL PUBLIC LICENSE. См. файл `LICENSE` для получения дополнительной информации.
```